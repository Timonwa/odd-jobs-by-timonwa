---
title: Product name, also used as the SEO title and h1. Max 50 characters. Required
titleAccent: the last words of the title, verbatim — the header gradients these. Required
eyebrow: Short label rendered above the title. Required
description: One sentence on what the buyer gets and the problem it solves. Max 155 characters. Required
keywords: # required
  - three to five search phrases
  - each one a phrase someone would type
  - not single words
category: Groups the product on cards and listings. Free text, so reuse an existing one where it fits. Required
publishedAt: "2026-01-01" # required; a future date hides it from any build
updatedAt: "2026-01-15" # optional — drop the line unless the product materially changed
canonicalUrl: "https://www.timonwa.com/shop/<the-matching-www-listing>" # required
price: 'Optional. Display text only — "Free" or "$10" reach the JSON-LD offer as USD; anything else (tiers, other currencies) publishes no price'
checkoutUrl: "https://buymeacoffee.com/timonwa/e/<id>" # required
checkoutLabel: Optional CTA label naming the storefront, e.g. Get it on Buy Me a Coffee
ogSubtitle: One line for the social card, read at thumbnail size. Max 100 characters. Required
ogPills: # required
  - Three words
  - Or fewer
  - Each
---

Body copy goes here.

## A section heading

More body copy.
