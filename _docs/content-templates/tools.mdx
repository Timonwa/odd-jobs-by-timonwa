{/* Page metadata lives in `TOOL_SEO`, not here. The `faq` export becomes
    FAQPage JSON-LD, so those answers are read with no page around them. */}

export const faq = [
	{
		question: "A question phrased the way someone would type it into Google?",
		answer:
			"The answer in the first sentence, then the detail. Two to four sentences, and never 'the tool above' or 'see below'.",
	},
	{
		question: "Is it free, and does anything I paste leave the page?",
		answer:
			"Worth answering on every tool — say which it is: computed in your browser, or sent to Gemini.",
	},
];

## A section heading

Body copy goes here.
